<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wms.mini.mapper.ShipDtlMapper">

    <select id="selectShipDtlList" parameterType="string" resultType="com.wms.mini.domain.ShipDtl">
        SELECT d.SHIP_NO, d.ITEM_CD, d.SHIP_QTY, i.ITEM_NM
        FROM SHIP_DTL d
                 JOIN ITEM_MST i ON d.ITEM_CD = i.ITEM_CD
        WHERE d.SHIP_NO = #{shipNo}
    </select>

    <insert id="insertShipDtl" parameterType="com.wms.mini.domain.ShipDtl">
        INSERT INTO SHIP_DTL (SHIP_NO, ITEM_CD, SHIP_QTY)
        VALUES (#{shipNo}, #{itemCd}, #{shipQty})
    </insert>

    <delete id="deleteShipDtlByShipNo" parameterType="string">
        DELETE FROM SHIP_DTL WHERE SHIP_NO = #{shipNo}
    </delete>

</mapper>