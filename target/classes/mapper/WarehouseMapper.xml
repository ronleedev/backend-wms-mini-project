<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wms.mini.mapper.WarehouseMapper">

    <select id="selectWarehouseList" resultType="com.wms.mini.domain.Warehouse">
        SELECT WH_CD, WH_NM, USE_YN
        FROM WAREHOUSE
        ORDER BY WH_CD
    </select>

    <select id="selectWarehouse" parameterType="string" resultType="com.wms.mini.domain.Warehouse">
        SELECT WH_CD, WH_NM, USE_YN
        FROM WAREHOUSE
        WHERE WH_CD = #{whCd}
    </select>

    <insert id="insertWarehouse" parameterType="com.wms.mini.domain.Warehouse">
        INSERT INTO WAREHOUSE (WH_CD, WH_NM, USE_YN)
        VALUES (#{whCd}, #{whNm}, #{useYn})
    </insert>

    <update id="updateWarehouse" parameterType="com.wms.mini.domain.Warehouse">
        UPDATE WAREHOUSE
        SET WH_NM = #{whNm},
            USE_YN = #{useYn}
        WHERE WH_CD = #{whCd}
    </update>

    <delete id="deleteWarehouse" parameterType="string">
        DELETE FROM WAREHOUSE WHERE WH_CD = #{whCd}
    </delete>

</mapper>
